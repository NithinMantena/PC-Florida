{
  "name": "pc-florida",
  "private": true,
  "type": "module",
  "description": "Florida P&C market data: hosted API (Supabase Edge Function), MCP server and OpenClaw CLI",
  "engines": {
    "node": ">=22.18"
  },
  "bin": {
    "flpc": "openclaw/bin/flpc.mjs"
  },
  "scripts": {
    "test:prep": "python etl/ingest.py --no-web --no-report --sqlite data/florida_pc.sqlite --bundle data/bundle.json.gz && python api/tests/parity_dump.py data/parity.json",
    "test": "node --test --test-concurrency=1 \"tests/**/*.test.ts\" \"tests/**/*.test.mjs\"",
    "check": "tsc -p tsconfig.json",
    "mcp": "node mcp/server.mjs",
    "flpc": "node openclaw/bin/flpc.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "postgres": "^3.4.7",
    "typescript": "^5.9.3"
  }
}
